# Public BFF origin for production/preview builds (inlined at build time).
# Leave unset for local dev: the Vite dev server proxies `/api` to your local BFF.
# Example: https://bff.mycompany.com  (no trailing slash; no /api suffix)
# VITE_API_BASE_URL=
